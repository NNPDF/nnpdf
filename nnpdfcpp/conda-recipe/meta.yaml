{% set libnnpdf_version = "  1.2.0b5.*" %}
package:
    name: nnpdf
    version: "{{ GIT_DESCRIBE_TAG[1:] }}.{{ GIT_DESCRIBE_NUMBER }}"

source:
    git_url: ../

requirements:
    build: 
        - toolchain # [osx]
        - libnnpdf {{libnnpdf_version}}
        - gcc # [linux]
        - isl # [linux]
        - yaml-cpp
        - cmake
        - apfel >=3
        - pkg-config
    run:
        - libnnpdf {{libnnpdf_version}}
        - apfel >=3
        - libgcc # [linux]
        - yaml-cpp

build:
    number: 0
    detect_binary_files_with_prefix: True

test:
    commands:
        - echo 

about:
    home: https://nnpdf.hepforge.org/
    license: SECRET
