#- process_type: DIS
  ##W2 is an "extra label" in the vp plotting notation
  ##w2min is loaded from input::datacuts.
  #reason: "Non perturbative effects at low W²"
  #rule: "W2 < w2min"

- dataset: ATLAS1JET11
  reason: "Because I wanted to"
  rule: "eta < 0.3"

- dataset: LHCBWZMU8TEV
  rule: "pto == 2 and etay >= 2.25"

- dataset: LHCBWZMU7TEV
  rule: "pto == 2 and etay >= 2.25"

- dataset: D0WMASY
  rule: "pto == 2 and etay >= 0.03"

- dataset: D0WEASY
  rule: "pto == 2 and etay >= 0.03"

- dataset: ATLASZPT7TEV
  rule: "False if (p_T2 < 30**2 or p_T2 > 500**2) else True"

- dataset: ATLASZPT8TEVMDIST
  rule: "p_T >= 30"

- dataset: ATLASZPT8TEVYDIST
  rule: "False if (p_T2 < 30**2 and p_T2 > 150**2) else True"

- dataset: CMSZDIFF12
  rule: "False if (p_T2 < 30**2 or p_T2 > 170**2 or etay > 1.6) else True"

- dataset: ATLASWPT31PB
  rule: "p_T > 30"

# - dataset: CMS_1JET_8TEV
#   rule: ""

- dataset: CMSDY2D11
  rule: >-
    False if ((pto in (0, 1) and 
    (pTmv > maxCMSDY2Dminv or pTmv < minCMSDY2Dminv or y > maxCMSDY2Dy))
    or ((pto == 2) and (pTmv > maxCMSDY2Dminv or y > maxCMSDY2Dy)))
    else True

- dataset: CMSDY2D12
  rule: >-
    False if ((pto in (0, 1) and 
    (pTmv > maxCMSDY2Dminv or pTmv < minCMSDY2Dminv or y > maxCMSDY2Dy))
    or ((pto == 2) and (pTmv > maxCMSDY2Dminv or y > maxCMSDY2Dy)))
    else True

- dataset: ATLASZHIGHMASS49FB
  rule: "pTmv <= maxCMSDY2Dminv"

# - dataset: LHCBLOWMASS37PB
#   rule: "pTmv <= maxCMSDY2Dminv"

# - dataset: ATLASLOMASSDY11
#   rule: "False if (pto in (0, 1) and idat < 6) else True"

- dataset: ATLASLOMASSDY11EXT
  rule: "False if (pto in (0, 1) and idat < 2) else True"

- dataset: DYE886P
  process_type: DYP
  local_variables:
    tau: M2 / sqrts**2
    ymax: -0.5 * np.log(tau)
  rule: "False if tau > maxTau or np.fabs(y/ymax) > maxY else True"

- dataset: DYE605
  process_type: DYP
  local_variables:
    tau: M2 / sqrts**2
    ymax: -0.5 * np.log(tau)
  rule: "False if tau > maxTau or np.fabs(y/ymax) > maxY else True"

- process_type: DIS_ALL
  reason: Generalised cut for all DIS processes
  rule: "False if Q2 <= q2min or Q2 * (1 - x) / x <= w2min else True"

- dataset: EMCF2P
  rule: "x > 0.1"

- dataset: EMCF2D
  rule: "x > 0.1"

- process_type: DIS_NCP_CH
  VFNS: FONLL-A
  rule: >-
    False if (Q2 <= Q2cut1_f2c or
    (Q2 <= Q2cut2_f2c and x <= xcut_f2c))
    else True
  
- process_type: DIS_NCP_CH
  VFNS: FONLL-C
  rule: "False if (Q2 <= 8 and ic) else True"
